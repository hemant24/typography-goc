var motionText = motionText || {}

(function(global){

	var Renderer = function(canvas){
		this.canvas = canvas
	}
	Renderer.prototype.render = function(stage){
		this._obj.push(obj)
	}
	
	motionText.Renderer = Renderer;
})(window)